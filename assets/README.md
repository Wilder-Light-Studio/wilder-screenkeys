# Assets

- `css/style.css` &mdash; the single stylesheet used by every page.
- `img/` &mdash; drop the product logo, favicon, and any inline imagery here. Reference them with paths like `assets/img/logo.png` from the HTML pages.

## To add before launch

- `img/favicon.ico` (linked from each page&rsquo;s `<head>`)
- `img/wilder-screenkeys-logo.png` (used on the homepage; copy from the dev repo&rsquo;s `assets/brand/`)
- `img/og-card.png` &mdash; 1200&times;630 social-share card (linked via `<meta property="og:image">`)
